"""Trilha D (DD-01): executores (local, Ray) e resume."""

from dataipsum.registry import Registry


def register(registry: Registry) -> None:
    """Vazio: a trilha D não registra geradores/sinks; ver `execution` para os executores."""
