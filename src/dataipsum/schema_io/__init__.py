"""Trilha F (DD-02): export/import de schema (DDL, Avro)."""

from dataipsum.registry import Registry


def register(registry: Registry) -> None:
    """Vazio: a trilha F não registra geradores/sinks."""
