"""Trilha A (DD-01): geradores de tipos (int, string, cpf, uuid, ...)."""

from dataipsum.registry import Registry


def register(registry: Registry) -> None:
    """Registra os geradores de tipo built-in. Vazio até a trilha A ser implementada."""
